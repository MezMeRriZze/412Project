Just run python SVD.py or decisionTree.py to get the result of using SVD or DT. The input should be in a folder called files, output will be in folder out.

The input should not include the first line, i.e. the csv column line.
